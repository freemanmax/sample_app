<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
                                  html: { method: :delete }, remote: true, :authenticity_token => true) do |f| %>
   <div class="actions"><%= f.submit "Unfollow" %></div>
<% end %>
