<%= form_for(@micropost) do |f| %>
   <%= render 'shared/error_messages', :object => f.object %>
   <div class="field">
     <%= f.text_area :content, placeholder: "Write post..." %>
   </div>
   <div class="actions">
     <%= f.submit "Create post" %>
   </div>
<% end %>
